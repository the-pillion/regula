services:
  regula:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pillion-regula
    ports:
      - "${REGULA_HTTP_PORT:-8083}:${REGULA_HTTP_PORT:-8083}"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/regula", "healthcheck"]
      interval: 10s
      timeout: 3s
      retries: 5
